@{
    ViewBag.Title = "Cargando llamadas";
}

<p>
    <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4">
            <h2>@ViewBag.Title</h2>
            <div class="container">
                <div class="loader"></div>
            </div>
        </div>
        <div class="col-md-4"></div>
    </div>
</p>

@section topLevel {
    @{ Html.RenderPartial("_Notifications.Modal"); }
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @{ Html.RenderPartial("_Notifications.js"); }
}